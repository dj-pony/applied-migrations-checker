============
Installation
============

At the command line::

    $ easy_install dj-pony.applied-migrations-checker

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-pony-applied-migrations-checker
    $ pip install dj-pony.applied-migrations-checker
